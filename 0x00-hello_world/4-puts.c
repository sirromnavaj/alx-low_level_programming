#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 *string initializing 
 *
 *writing to stdout
 *
 * return always 0 (Success)
*/
int main(void)
{	
char str1[60] = "\"programming is like building a multilingual puzzle";
puts(str1);
return (0);
}
